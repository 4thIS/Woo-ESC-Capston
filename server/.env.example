# 메인Pi 서버 — 값을 채워 .env 로 저장 (pre-commit 이 .env 커밋을 막는다)
SERVER_DB=main.db
STATUS_HOUR_UTC=18
JWT_SECRET=
JWT_TTL_H=24
STUDENT_WEB_URL=
MAIL_BACKEND=smtp
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=
DEBUG=0
CORS_ORIGINS=
