# <기능명> — 설계 (spec)

<!-- 배치: docs/specs/YYYY-MM-DD-<feature-slug>-design.md -->
<!-- brainstorming 스킬로 작성. "무엇을·왜"만. 구현 방법(어떻게)은 plan에서. -->

- 생성일시: YYYY-MM-DD
- 수정일시: YYYY-MM-DD

## 0. 배경 · 위치

(이 기능이 왜 지금 필요한가. 현재 상태·전례. v2 전체 설계는
`docs/specs/2026-09-09-lora-v2-wor-design.md`를 참조하고, 이 문서는 그중 어느 부분을 구체화하는지 밝힌다.)

## 1. 목표 · 비목표

### 목표
- (목표 1)
- (목표 2)

### 비목표 (이번엔 안 함 / 후속)
- (비목표 1)

## 2. 데이터 · 계약 (있다면)

(신규/변경되는 계약물. 없으면 "없음".)

- `lora_proto/` 프로토콜 정의 변경: 예 / 아니오 → 있으면 lockstep 필요 (계약 PR 먼저 머지)
- DB 스키마·마이그레이션 변경: 예 / 아니오 → 있으면 lockstep 필요
- 서버 응답 스키마(웹이 소비) 변경: 예 / 아니오 → 있으면 additive 여부와 웹 담당 협의 결과 명시

## 3. 접근 제어 / 제약

- (권한·검증·엣지 케이스 규칙. 전력 예산·에어타임·패킷 크기 같은 물리 제약도 여기.)

## 4. 인터페이스 계약

| 메서드·경로 / 함수 / 프레임 TYPE | 접근 | 설명 |
|---|---|---|
| (예: `POST /api/reservations`) | (관리자) | (설명) |

## 5. 영역별 영향

- firmware: (무엇을)
- server: (무엇을)
- web: (무엇을)
- lora_proto: (무엇을 / 없음)

## 6. 무회귀 · 롤아웃

- (기존 동작 보존 방법, 배포 순서. 계약 변경 시 lockstep 순서를 명시.)
- (단말이 이미 필드에 나가 있다면 구버전 펌웨어와의 호환을 어떻게 지키는지.)

## 7. 역할 분담

| 영역 | 담당 |
|------|------|
| firmware (무선·절전·스케줄링) | cw @ssenu |
| firmware (렌더링·화면) | dh @Hyeon02-kr |
| server / web (풀스택) | wj @leemonta9482 |
| web (퍼블리싱·디자인시스템) | mh @jmh7706jmh-ops |
| 계약(`lora_proto/`)·인프라 | cw @ssenu |

## 8. 성공 기준

- (완료를 어떻게 확인하는가 — 관찰 가능한 기준. 하드웨어라면 실측 수치로.)

## 9. 열린 결정 (plan 단계에서 확정)

- (열린 질문 1)
